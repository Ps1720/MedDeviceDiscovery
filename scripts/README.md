# scripts

Operational helper scripts.

Planned:
- `demo_reset.sh` — reset HAPI/app state between demo runs.
- `load_test_devices.sh` — pre-load known UDIs for the demo.
- `generate_capability_statement.py` — emit `docs/capability-statement.json`.
