# Environment variable files — never commit real credentials
.env
.env.*
!.env.example

# Editor / IDE
.vscode/
.idea/
*.swp

# Internal working notes (kept locally, not part of the deliverable)
Build.md
explanation.md
project_abstract.md
Suggestions.md

# Python
__pycache__/
*.py[cod]
.venv/
venv/
ENV/
env/

# Databases / caches
*.sqlite
*.db
.pytest_cache/
.coverage

# Generated QR codes (regenerable cache)
services/gudid-core/static/qr_codes/*.png

# OS
.DS_Store

# Synthea / HAPI generated data
infrastructure/synthea/output/
infrastructure/synthea/*.jar
infrastructure/hapi/data/

# Eval data populated during pilot (keep .gitkeep, ignore real logs)
eval/usage_logs/*.csv

# CDS Hooks recall cache (regenerated at runtime)
services/cds-hooks/data/*.db
backups/

# Submission draft and its Word export — working documents, not part of the deliverable
abstract.md
PeriopUDI-Submission.docx
~$*
*.tmp
